# How to Use copilot-skills-pack

This guide has been split into focused docs:

| If you want to … | Read |
|---|---|
| Install the pack (prerequisites, OS-specific steps, Figma PAT, troubleshooting) | **[docs/INSTALL.md](./docs/INSTALL.md)** |
| Use the core lifecycle — `/spec` `/plan` `/build` `/test` `/review` `/code-simplify` `/ship`, plus the `code-reviewer` / `test-engineer` / `security-auditor` personas | **[docs/SKILLS_CORE.md](./docs/SKILLS_CORE.md)** |
| Use the Figma → React workflow — `/ui-learn` `/ui-refine` `/ui-spec` `/ui-plan` `/ui-build` `/ui-flag` `/ui-mcp-status`, the `frontend-craftsman` persona, re-skin mode, setup for new vs existing repos | **[docs/SKILLS_UI.md](./docs/SKILLS_UI.md)** |

The original single-file walkthrough was replaced to keep each doc
focused and small.
